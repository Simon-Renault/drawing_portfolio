---
label: home_page
hide_body: true
display_field: title
fields:
- name: hero_title
  type: text
  config:
    required: false
  label: Hero title
- name: hero_image
  type: file
  config:
    maxSize: 64
  label: hero image
- name: item
  type: blocks
  template_types:
  - drawing
  - quote
  config:
    min: 
    max: 
  label: Item
  description: All the items presented in the home page gallery
pages:
- config/home_config.json
