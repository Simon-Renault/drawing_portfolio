---
label: home_page
hide_body: true
display_field: title
fields:
- name: hero_title
  type: text
  config:
    required: false
  label: Hero title
- name: hero_image
  type: file
  config:
    maxSize: 64
  label: hero image
- name: items_1
  type: blocks
  template_types:
  - quote
  - drawing
  config:
    min: 
    max: 
  label: items
pages:
- config/home_config.json
