---
label: home_page
hide_body: true
display_field: title
fields:
- name: item
  type: blocks
  template_types:
  - home_page
  - drawing
  config:
    min: 
    max: 
  label: Item
  description: All the items presented in the home page gallery
pages:
- config/home_config.json
