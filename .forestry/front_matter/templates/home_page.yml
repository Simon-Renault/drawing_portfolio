---
label: home page
hide_body: true
fields:
- name: hero_title
  type: text
  config:
    required: false
  label: Hero title
- name: hero_image
  type: file
  config:
    maxSize: 64
  label: hero image
- name: items
  type: blocks
  template_types:
  - quote
  - home-page-item
  config:
    min: 
    max: 
  label: items
pages:
- config/home_config.json
